# Airflow providers (use versions from constraint file)
apache-airflow-providers-google==11.0.0
apache-airflow-providers-postgres==5.14.0

# GCP dependencies (let constraint file handle versions)
google-cloud-storage
google-cloud-bigquery
google-auth

# dbt (these may need adjustment if constraints conflict)
dbt-core==1.9.0
dbt-bigquery==1.9.0

# HTTP client (let constraint file handle version)
requests

# Utilities
python-dotenv==1.0.1
